package wd;

import io.github.bonigarcia.wdm.WebDriverManager;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.testng.annotations.Test;

import javax.xml.xpath.XPath;

public class Locators {
    @Test
    public void locatorsTest () {
       // WebDriverManager.chromedriver().setup();

//        WebDriver driver = new ChromeDriver();

  //      driver.findElement(By.className("large-2 columns")).findElement(By.name("button")).findElement(By.name("button alert")).findElement(By.name("button success")).click();

    //    WebElement element = driver.findElement(By.partialLinkText("Definiebas0"));
      //  element.getAttribute();


        //driver.findElement(By.cssSelector(".large-2.columns .button"));
       // driver.findElement(By.xpath(String.format("//table//tr[1]/td[4]")));

       // driver.findElement(By.xpath(String.format("//label[text()='%s]/../input", "Password")));
       // driver.findElement(By.xpath(String.format("//label[text()='%s]/../input", "Username")));


       // driver.findElement(By.cssSelector(""));
    }
}
